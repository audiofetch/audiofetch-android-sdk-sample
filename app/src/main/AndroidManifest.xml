<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.audiofetch.aflib">

    <application
        android:name="com.audiofetch.afaudiolib.bll.app.ApplicationBase"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Waio">
        <activity
            android:name=".uil.activity.MainActivity"
            android:taskAffinity=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize"
            android:largeHeap="true"
            android:allowClearUserData="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep linking -->
            <intent-filter android:label="@string/filter_title_view_audiofetch">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="www.audiofetch.com"
                    android:pathPrefix="/android"
                    android:scheme="http" />

                <data
                    android:scheme="audiofetch"
                    android:host="android" />
            </intent-filter>
        </activity>
        <activity
            android:name=".uil.activity.ExitActivity"
            android:excludeFromRecents="true"
            android:taskAffinity=".MainActivity"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay" />
    </application>

</manifest>